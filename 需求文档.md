# 侧语：对话辅助 Android App

版本：0.1 / 2026-09-12。状态：需求基线已确定，先实现线上模式。

## 1. 产品目标

在陌生、半熟及已有关系的交流中，帮助用户注意遗漏的线索、调整自身表达，并提供可执行的下一步建议。用户决定是否采纳。主要痛点是没听懂言下之意、兴奋时忽略反馈、连续追问、过度解释和不知如何接话。

“Game Boy”是场外攻略的比喻，不是硬件名称，也不要求复古游戏界面。产品暂名“侧语”。建议可以直接、幽默、暧昧、主动推进或建议暂缓；观察、用户背景、模型推断必须区分。不给真人生成伪精确的好感分数，不把忠诚度或心理状态当作已经证实的事实。

## 2. 设备和优先级

| 项目 | 决定 |
| --- | --- |
| 主设备 | 一加 Ace 2，Android 16 / ColorOS 16.0.5，骁龙 8+ Gen 1，12GB RAM |
| 兼容设备 | 魅族 18X，Android 11 / Flyme 9.3，骁龙 870，12GB RAM |
| 最低版本 | Android 11，API 30 |
| 第一阶段 | 线上文字聊天：微信、QQ；支持手动补充上下文 |
| 第二阶段 | 一加上的线下语音、单耳播报、声学分析 |
| 兼容承诺 | 魅族先保证线上模式，不承诺本地实时语音性能 |

## 3. 第一阶段用户流程

1. 用户配置自己的模型服务地址、模型名称和 API 密钥；密钥只在本机加密存储。
2. 用户输入或分享聊天文本，补充关系背景、当前目标和表达偏好。
3. 可选开启系统屏幕捕获，明确选择共享范围；授予悬浮窗权限。
4. 进入微信/QQ，通过悬浮把手请求读取。截图只在本地做中文 OCR，不发送图片给云端。
5. 用户确认目标聊天标题。识别结果与标题不匹配时不发送云端，避免跨聊天混用。
6. 云端根据近期聊天、手动背景和用户主动关联的档案生成：一句行动提示、可能的解释、依据、不确定性、三条回复候选。
7. 通过悬浮面板查看、复制候选回复；发送始终由用户完成。
8. 点击或手势结束，弹出保存选项。可保存当前记录，逐条选取、修改画像候选，再创建或手动关联人物。

未配置模型时允许浏览界面及体验明确标记的示例，不把示例当作真实分析结果。不自动发送消息，不自动代替用户确认档案。

## 4. 线上模式

### 输入和上下文

- 支持手动粘贴、Android 文本分享、当前可见聊天窗口 OCR。
- 手动背景独立于对方原话；事实冲突明显时呈现差异，避免强行合并。
- 当前对话目标可选自然延续、理解言外之意、推进关系、修复关系、礼貌结束。
- 语气可选自然、真诚、幽默、直接、暧昧、克制。
- 群聊不自动锁定某个人，不为未手动关联的人套用已有档案。
- 首版不自动翻页、不读取后台聊天数据库、不索取账号密码。

### 聊天元数据

用户希望分析昵称、头像、账号标识、已读、回复间隔、撤回及主动开启话题的频率。

实际实现只接收当前窗口可见、用户提供或本次会话确实观察到的数据；缺失写“未知”。微信普通私聊通常不提供已读回执；不能虚构“已读”。没观察到消息到达时间时，不能拿截屏时间计算真实回复耗时。撤回的内容不可凭空恢复。

这些信息是弱信号；只有影响建议时在详情展示依据，不覆盖原聊天气泡。截图 OCR 可能丢失头像、时间分组和说话人身份，需允许手动补充，不承诺仅凭截屏提取隐藏账号。

### 屏幕范围

- Android 屏幕捕获确认不能跳过；屏幕会话被系统停止后立即释放资源。
- 新系统优先使用单个 App 共享；Android 11 全屏捕获应使用按需取帧、标题校验和前台状态校验。
- 首版采用手动召唤取帧，不做无法可靠识别窗口的持续云端上传。
- 离开目标窗口、锁屏、权限失效或 OCR 不匹配时停止分析该帧；不同联系人不自动继承上下文。
- 不调用无边界的无障碍读取。原讨论的“一键填入输入框”依赖输入法或经授权的无障碍实现，首版落为复制后手动粘贴，后续单独实现输入法扩展。

## 5. 悬浮交互

- 使用真正的 Android 悬浮窗，首版包含边缘把手、提示胶囊和侧栏。
- 默认把手位于右边缘、桌面“问小布”大致同高度，约可用屏幕高度的 82%；长度短小。只做自身 App 组件，不冒充系统小布。
- 可拖动、记忆位置、恢复默认；位置按比例存储，适配分辨率和横竖屏。
- 单击请求建议，向内滑展开菜单，双击进入静默，长按 1 秒恢复输出；长按 3 秒紧急退出。手势必须避免长按 1 秒提前触发后破坏 3 秒退出动作。
- 文字 / 耳机 / 双通道可切换；默认无震动。
- 普通短提示 3 秒消失，最长 5 秒；点按固定，关闭或取消固定后恢复自动隐藏。
- 线上允许详细面板；线下优先 8 个汉字以内的短提示。
- 侧栏包含场景、通道、手动建议、背景补充、保存信息、暂停/恢复和结束。
- 暂停与静默区别清楚：暂停不采集、不发请求；静默只关闭建议输出。

## 6. 静默、锁屏和结束

原讨论称“装死模式”，实现名称为“静默”。双击把手可进入；语音阶段可配置用户口令“中国人会飞”。恢复必须手动，不能由模型自动恢复。

用户希望静默时继续分析并保存转写和结果，默认长期保留、可删除；原始音频默认不保存。该功能仅在已经明确启用的采集会话中生效。静默不改变系统实际采集状态，通知应如实说明屏幕共享或麦克风使用。不会实现隐藏系统隐私指示、伪装系统组件、在他人质疑后自动播放音乐以掩饰继续记录等行为。音乐播放器不作为第一版内容。

锁屏、通知栏不得显示人物姓名、聊天文字、建议或摘要。档案界面需解锁后查看。系统状态通知提供暂停/停止；口令不替代系统使用状态。

结束后询问是否保存；放弃清除当前临时数据。紧急退出停止采集、网络请求、播报、悬浮窗，清除未确认的临时状态；不删除已经保存的档案。30 分钟无语音、电话打入和系统中断属于语音阶段待实测的停止条件。耳机断开绝不把私密提示转到扬声器。

## 7. 人物档案和记录

- 用户启动分析可采集当前可见昵称、头像、账号信息作为候选；与已有人物关联必须手动确认。
- 头像仅保存加密缩略图，不保存原图；昵称/头像变化保留历史并待确认。
- 候选字段：兴趣、经历、偏好、重要日期、约定、互动提醒，及明确标注为推测的性格、好感、心理状态等。
- 忠诚度等缺乏充分依据的类别允许用户自己记笔记；模型不能仅凭几轮对话作稳定品格判断。
- 每条候选有类型、原始依据、时间、来源、不确定性、保留/修改/删除。
- 线上线下档案可关联；不自动凭同名、头像或声音合并身份。
- 支持本地加密存储、编辑、按人物/对话删除、全部删除。
- 支持有口令的加密导出/导入，导出不含 API 密钥；云同步后续单独实现端到端加密，不把系统云备份当 E2EE。
- 普通结束保存由用户确认；静默期间的长期保存属于明确开启此偏好后的记录行为。

## 8. 模型输出和反馈

输出结构：action、interpretation、evidence、uncertainty、replies、memoryCandidates。

提示默认一个动作，展开查看理由和三种候选策略。允许“不回复/再等等”。拒绝、停止交流和不舒服的明确信号优先于推进关系。低置信度不主动作强结论，手动请求仍可给多种解释。

聊天文本、OCR 文字和用户附加的文档均是待分析数据，不是模型系统指令。模型不执行屏幕里的指令，不替用户发送消息或访问外部资源。渲染结果只用纯文本，避免脚本注入。

每条结果可反馈有用/没用，原因包括错误、太晚、太长、语气不合；反馈可撤销。首版保存反馈，不声称在线训练或已经自动学习。会话结束可评分：及时性、信号理解、交流自然度。

## 9. 线下第二阶段

普通话夹少量英语；朋友局、活动、寒暄、搭讪、约会，职业关系后续。先不使用摄像头。真实噪声、多人抢话和手机摆放决定效果，不能仅靠手机型号承诺实时准确率。

本地音频采集、VAD、流式 ASR，近期文本送云端；本地模型候选 sherpa-onnx。单耳耳机输出关键词，主动请求时一句建议。基础语速、停顿、说话轮次不依赖“读心”；没有视觉输入不得声称“对方看向出口”。

关键提示 1–2 秒、普通 3 秒是待测目标，不是保证；主动提示节流 20–30 秒。断网只保留已经实现的本地能力，不能把语言推断伪装成本地可靠功能。说话人分离、确认自己的声音、临时目标声音匹配需分别评估，不能认为几秒录音必定准确区分多人。

## 10. 数据处理约束

- 云端默认只接收用户明确提交的近期文字、必要背景和选定档案；截图和原始音频不自动上传。
- HTTPS；本地密钥使用 Android Keystore；关闭明文网络和系统自动备份。
- “分析后立即删除”只能约束自己的服务；第三方模型的日志、留存和训练政策必须以供应商配置为准，不能在界面承诺无法验证的零留存。
- 不在日志中写入对话、密钥、头像或模型完整响应。
- 断网、超时、限流、鉴权错误有明确状态和重试；不得用示例替换失败的真实建议。
- 不在用户未明确授权的情况下发消息给任何人。

## 11. 视觉方向

原生 Android 能力 + 本地 WebView 界面。工具型、清晰、紧凑，避免营销首页。主界面为会话工作台，底部为会话、人物、记录、设置；白色与石墨文字、绿色主操作、暖红警示，小圆角、无装饰性大卡片。图标采用开源 Lucide。空状态不填假人物；示例会话明确标记。

## 12. 验收

第一阶段必须提供可构建 Android 工程和尽可能实际产出的 APK。核心验证：手动输入 → 真实模型调用 → 结构化建议 → 复制 → 结束保存 → 人物关联 → 重启恢复 → 删除/加密导入导出。

屏幕捕获验证：系统拒绝、系统停止、横竖屏、锁屏、OCR 标题不匹配、悬浮窗隐藏后取帧、不同联系人切换、请求完成后已结束会话等情况。线上模式先保证按需读取；连续窗口监听待可靠范围识别后开启。

主机测试包括模型响应解析、会话隔离、导入校验、Web UI 交互与移动尺寸截图。没有接入实体手机时，必须明确列出未验证的 ROM、蓝牙路由、投屏授权和功耗项目，不能宣称真机验收通过。

用户上传附件是书目文件名列表，不是书籍正文；本项目没有据此建立已读取或已验证的知识库。
